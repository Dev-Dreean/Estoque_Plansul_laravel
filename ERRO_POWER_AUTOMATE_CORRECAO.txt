╔════════════════════════════════════════════════════════════════════════════╗
║          🚨 ERRO NO SEU POWER AUTOMATE - CORREÇÃO URGENTE               ║
╚════════════════════════════════════════════════════════════════════════════╝

🔴 PROBLEMA IDENTIFICADO:
─────────────────────────────────────────────────────────────────────────────
Você colocou no Body da ação HTTP:

```json
{"from":"@{triggerOutputs()?['body/from']}","subject":"..."}
```

❌ Isso NUNCA vai funcionar! Power Automate não interpreta expressions
   escritas manualmente dentro de strings JSON!

═══════════════════════════════════════════════════════════════════════════════

✅ MÉTODO CORRETO - SIGA PASSO A PASSO:
─────────────────────────────────────────────────────────────────────────────

**OPÇÃO 1 - Modo Simples (RECOMENDADO):**

1. Na ação HTTP, no campo **Body**, delete TUDO que está lá

2. Digite apenas o caracter `{` e pressione ENTER

3. Digite: `"from": "`

4. Clique no ícone ⚡ (raio) que aparece ao lado

5. No painel que abre, selecione o campo **De** (do trigger)

6. Continue digitando: `",` e ENTER

7. Digite: `"subject": "`

8. Clique ⚡ e selecione **Assunto**

9. Digite: `",` e ENTER

10. Digite: `"body": "`

11. Clique ⚡ e selecione **Corpo**

12. Digite: `"`

13. ENTER e digite `}`

**RESULTADO VISUAL ESPERADO:**

```
{
"from": [ícone De],
"subject": [ícone Assunto],
"body": [ícone Corpo]
}
```

Os ícones azuis/cinza são os campos dinâmicos - NÃO digite texto!

═══════════════════════════════════════════════════════════════════════════════

**OPÇÃO 2 - Expression (se Opção 1 não funcionar):**

1. No campo Body, clique em ⚡

2. Clique na aba **Expression** (no topo)

3. Cole EXATAMENTE isto:

```
json(concat('{"from":"', replace(triggerOutputs()?['body/from'], '"', '\\"'), '","subject":"', replace(triggerOutputs()?['body/subject'], '"', '\\"'), '","body":"', replace(replace(triggerOutputs()?['body/body'], '"', '\\"'), char(10), '\\n'), '"}'))
```

4. Clique OK

═══════════════════════════════════════════════════════════════════════════════

🔍 COMO VERIFICAR SE ESTÁ CORRETO:
─────────────────────────────────────────────────────────────────────────────

Após configurar:

1. Clique nos **...** da ação HTTP
2. Clique em **Exibir código** ou **View code**
3. Procure a seção "body"

**✅ CORRETO - deve estar assim:**
```json
"body": {
  "from": "@{triggerOutputs()?['body/from']}",
  "subject": "@{triggerOutputs()?['body/subject']}",
  "body": "@{triggerOutputs()?['body/body']}"
}
```

**❌ ERRADO - se estiver assim:**
```json
"body": "{\"from\":\"@{triggerOutputs()?['body/from']}\",..."
```
☝️ Está em formato STRING (entre aspas) - REFAÇA!

═══════════════════════════════════════════════════════════════════════════════

📧 TESTE DEPOIS DE CONFIGURAR:
─────────────────────────────────────────────────────────────────────────────

1. Salve o Flow

2. Envie email com este conteúdo:

   ─────────────────────────────────────────────────────────
   Para: suporte.dev@plansul.com.br
   Assunto: Solicitacao de Bem
   
   Corpo:
   Solicitante: João Silva Teste
   Matricula: 99999
   Projeto: 100 - Teste
   UF: SP
   Setor: TI
   Local destino: Almoxarifado
   Observacao: Teste corrigido
   
   Itens:
   - Monitor 24"; 1; UN; Teste
   - Mouse; 1; UN; Teste
   ─────────────────────────────────────────────────────────

3. Aguarde 1 minuto

4. Vá em "Meus fluxos" → Clique no flow → Veja a última execução

5. Expanda a ação **HTTP** e veja os **Inputs**

**✅ DEVE MOSTRAR:**
```json
{
  "from": "seuemail@empresa.com",
  "subject": "Solicitacao de Bem",
  "body": "Solicitante: João Silva Teste\nMatricula: 99999..."
}
```

**❌ SE MOSTRAR CAMPOS VAZIOS:**
```json
{
  "from": "",
  "subject": "",
  "body": ""
}
```
☝️ Significa que não usou campos dinâmicos! REFAÇA usando Opção 1!

═══════════════════════════════════════════════════════════════════════════════

⚠️ IMPORTANTE:
─────────────────────────────────────────────────────────────────────────────

• NÃO copie e cole código JSON com @{...} - Power Automate não interpreta!
• USE o ícone ⚡ para adicionar campos dinâmicos
• Campos dinâmicos aparecem como ÍCONES azuis/cinza na tela
• Se digitar manualmente @{triggerOutputs...}, vai ficar como TEXTO!

═══════════════════════════════════════════════════════════════════════════════

📊 RESPOSTA ESPERADA (se tudo certo):
─────────────────────────────────────────────────────────────────────────────

Status: **200 OK**

Body:
```json
{
  "success": true,
  "message": "Solicitacao registrada com sucesso.",
  "solicitacao_id": 123
}
```

E a solicitação aparecerá em: https://plansul.info/solicitacoes

═══════════════════════════════════════════════════════════════════════════════

🆘 SE AINDA NÃO FUNCIONAR:
─────────────────────────────────────────────────────────────────────────────

Tire print/captura de:

1. Configuração da ação HTTP (mostrar o campo Body com os ícones)
2. View code da ação HTTP (seção "body")
3. Inputs da última execução
4. Output/erro da última execução

E me envie para eu diagnosticar exatamente o que está errado!

═══════════════════════════════════════════════════════════════════════════════

Data: 26/01/2026 17:00
Status: ✅ Backend pronto | ⏳ Aguardando configuração correta do Power Automate
